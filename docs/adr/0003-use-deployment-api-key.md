# 使用部署级单一 API Key

生成的后端始终使用环境变量提供的部署级单一 API Key 保护业务 API，不建立用户或客户端身份，也不提供关闭鉴权的 Copier 选项；健康检查等基础探针保持公开。删除 JWT、refresh token、注册登录、密码、session、用户模型、RBAC 和用户管理接口；该取舍以安全默认的服务间调用边界替代面向终端用户的身份系统，未来若需要客户端级密钥管理，应作为新的身份模型重新设计。
