# Issue #7 减法迁移追溯。每个 target 必须在等价 manifest 中登记内容摘要。

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "backend 移至项目根", "删除 V1 排除依赖", "依赖兼容修改"]
removed_references = ["用户/JWT、frontend、分页、替代队列与 telemetry 依赖"]
approved_deviation = ["deployment API Key 固定启用", "FastAPI <0.137 以保留 legacy routes CLI；runtime smoke 复现 0.137 lazy router 不兼容"]
verification = ["test_minimal_answers_render_deterministic_project_metadata", "test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.env.example"
target = ".env.example"
allowed_transformations = ["Copier 模板语法", "删除关闭能力的环境变量"]
removed_references = ["JWT、用户、frontend、文件、RAG 与其他删除能力变量"]
approved_deviation = ["LLM_* 使用批准的 OpenAI Chat Completions-compatible 接口"]
verification = ["test_minimal_tree_contains_only_legacy_core_service"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/main.py"
target = "app/main.py"
allowed_transformations = ["Copier 模板语法", "删除 V1 排除生命周期", "关闭能力条件化"]
removed_references = ["分页、SecurityHeaders、用户/JWT、RAG、消息渠道、admin"]
approved_deviation = ["health probes 挂载到公开根路径", "Logfire 仅由后续 Agent slice 接入"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/config.py"
target = "app/core/config.py"
allowed_transformations = ["Copier 模板语法", "删除能力字段", "可选基础设施条件化"]
removed_references = ["身份、frontend、billing、文件、RAG、provider 与删除能力 Settings"]
approved_deviation = ["deployment API Key 替代用户/JWT", "LLM_* 使用批准的单一模型接口"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/exceptions.py"
target = "app/core/exceptions.py"
allowed_transformations = ["删除身份与排除能力异常", "可选能力条件化"]
removed_references = ["AuthenticationError、AuthorizationError、PaymentRequiredError"]
approved_deviation = ["APIKeyMissingError 与 InvalidAPIKeyError 承载 deployment API Key 边界"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/logging.py"
target = "app/core/logging.py"
allowed_transformations = ["删除用户/JWT 脱敏模式", "保留凭据脱敏"]
removed_references = ["email、JWT、Bearer、refresh token 模式"]
approved_deviation = ["无"]
verification = ["test_minimal_tree_contains_only_legacy_core_service"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/middleware.py"
target = "app/core/middleware.py"
allowed_transformations = ["删除未接线 SecurityHeaders"]
removed_references = ["SecurityHeadersMiddleware"]
approved_deviation = ["无其他差异"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/deps.py"
target = "app/api/deps.py"
allowed_transformations = ["删除身份与未启用能力依赖", "deployment API Key 替换"]
removed_references = ["CurrentUser、JWT、分页、Resource service dependencies"]
approved_deviation = ["deployment API Key 始终启用且使用 constant-time compare"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/exception_handlers.py"
target = "app/api/exception_handlers.py"
allowed_transformations = ["删除 WebSocket 分支", "删除 JWT challenge"]
removed_references = ["HTTPConnection WebSocket handling", "WWW-Authenticate: Bearer"]
approved_deviation = ["deployment API Key 错误不发送 Bearer challenge"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/health.py"
target = "app/api/health.py"
allowed_transformations = ["根路径迁移", "删除关闭资源 probes"]
removed_references = ["database、Redis、RAG、LLM provider、billing、Celery probes 与 /ready alias"]
approved_deviation = ["无基础设施 minimal readiness 返回空 checks"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/schemas/base.py"
target = "app/schemas/base.py"
allowed_transformations = ["删除未使用 schema"]
removed_references = ["TimestampSchema、AgentModelsResponse 与通用 response schema"]
approved_deviation = ["仅保留 health response contract"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/services/health.py"
target = "app/services/health.py"
allowed_transformations = ["根目录迁移"]
removed_references = ["无"]
approved_deviation = ["无差异"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/cli/commands.py"
target = "cli/commands.py"
allowed_transformations = ["Copier 模板语法", "删除未启用能力命令"]
removed_references = ["用户、Celery、database 与 Taskiq commands（后两者由后续 slice 恢复）"]
approved_deviation = ["server commands 始终生成"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract"]

# Issue #8 PostgreSQL 与 Item CRUD profile。profile 参与 manifest key，避免不同 ORM
# 对同一路径的渲染结果互相覆盖。

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "删除 V1 排除依赖", "保留 PostgreSQL 与 SQLAlchemy 依赖"]
removed_references = ["用户/JWT、frontend、排除能力依赖"]
approved_deviation = ["psycopg2-binary 仅供 Alembic 同步 URL", "依赖版本按当前兼容范围归一化"]
verification = ["test_postgresql_questions_and_orm_rendering_follow_legacy_contract", "test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "删除 V1 排除依赖", "保留 PostgreSQL 与 SQLModel 依赖"]
removed_references = ["用户/JWT、frontend、排除能力依赖"]
approved_deviation = ["SQLModel 与 SQLAlchemy 分支互斥", "依赖版本按当前兼容范围归一化"]
verification = ["test_postgresql_questions_and_orm_rendering_follow_legacy_contract", "test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/main.py"
target = "app/main.py"
allowed_transformations = ["Copier 模板语法", "删除 V1 排除生命周期", "保留数据库资源关闭"]
removed_references = ["用户/JWT、RAG、消息渠道、billing、admin 生命周期"]
approved_deviation = ["数据库 engine 在 session 模块初始化，lifespan 仅负责 close_db"]
verification = ["test_postgresql_cli_health_and_lifespan_runtime_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/base.py"
target = "app/db/base.py"
allowed_transformations = ["Copier 模板语法", "仅保留 SQLAlchemy 分支"]
removed_references = ["无"]
approved_deviation = ["无差异"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/base.py"
target = "app/db/base.py"
allowed_transformations = ["Copier 模板语法", "仅保留 SQLModel 分支"]
removed_references = ["无"]
approved_deviation = ["Base 保留 SQLModel.metadata 公开入口"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/session.py"
target = "app/db/session.py"
allowed_transformations = ["Copier 模板语法", "仅保留 SQLAlchemy AsyncSession import"]
removed_references = ["Celery 与 ARQ 文案"]
approved_deviation = ["可选 Logfire instrumentation 由条件块接入"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/session.py"
target = "app/db/session.py"
allowed_transformations = ["Copier 模板语法", "AsyncSession 替换为 SQLModel 实现"]
removed_references = ["Celery 与 ARQ 文案"]
approved_deviation = ["共享 transaction contract 保持不变"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/deps.py"
target = "app/api/deps.py"
allowed_transformations = ["删除身份依赖", "deployment API Key 替换", "恢复 DBSession"]
removed_references = ["CurrentUser、JWT、分页依赖"]
approved_deviation = ["DBSession 以 function scope 绑定 get_db_session，确保事务在响应发送前完成"]
verification = ["test_postgresql_cli_health_and_lifespan_runtime_contract", "test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/deps.py"
target = "app/api/deps.py"
allowed_transformations = ["删除身份依赖", "deployment API Key 替换", "恢复 SQLModel DBSession"]
removed_references = ["CurrentUser、JWT、分页依赖"]
approved_deviation = ["DBSession 使用 SQLModel AsyncSession 并绑定 function-scoped get_db_session"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/health.py"
target = "app/api/health.py"
allowed_transformations = ["根路径迁移", "仅保留 PostgreSQL readiness"]
removed_references = ["Redis、RAG、LLM provider、billing、Celery probes"]
approved_deviation = ["readiness 通过 DBSession 执行 SELECT 1"]
verification = ["test_postgresql_cli_health_and_lifespan_runtime_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/health.py"
target = "app/api/health.py"
allowed_transformations = ["根路径迁移", "仅保留 PostgreSQL readiness", "使用 SQLModel select 与 exec"]
removed_references = ["Redis、RAG、LLM provider、billing、Celery probes"]
approved_deviation = ["readiness 通过 SQLModel DBSession 执行 SELECT 1"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/alembic/env.py"
target = "alembic/env.py"
allowed_transformations = ["backend 移至项目根", "删除排除 model imports", "保留 Item metadata import"]
removed_references = ["用户、billing、RAG、消息渠道与其他删除模型"]
approved_deviation = ["online migration 使用 Settings.DATABASE_URL"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/cli/commands.py"
target = "cli/commands.py"
allowed_transformations = ["删除排除命令", "保留 server 与 database commands"]
removed_references = ["用户、Celery、Taskiq 与 custom commands"]
approved_deviation = ["database 命令保留 legacy 名称与参数"]
verification = ["test_postgresql_cli_health_and_lifespan_runtime_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/router.py"
target = "app/api/router.py"
allowed_transformations = ["删除 API versioning 与排除 routes", "挂载 Item router"]
removed_references = ["用户、billing、RAG、消息渠道、admin routes"]
approved_deviation = ["deployment API Key 由 api_router 统一保护"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/models/item.py"
target = "app/db/models/item.py"
allowed_transformations = ["Copier 模板语法", "删除 owner 外键与 relationship"]
removed_references = ["owner_id、users.id ForeignKey、User relationship"]
approved_deviation = ["Item 成为无身份资源"]
verification = ["test_item_crud_switch_controls_the_complete_generated_slice", "test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/db/models/item.py"
target = "app/db/models/item.py"
allowed_transformations = ["Copier 模板语法", "迁移到 SQLModel table model", "删除 owner 外键与 relationship"]
removed_references = ["owner_id、users.id ForeignKey、User relationship"]
approved_deviation = ["SQLModel 分支使用 Field 与 Column 表达同一表结构"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/repositories/item.py"
target = "app/repositories/item.py"
allowed_transformations = ["删除 owner 与 list 查询", "保留 flush 与 refresh"]
removed_references = ["owner_id、list_for_owner、count、分页"]
approved_deviation = ["事务由 session manager 独占"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/repositories/item.py"
target = "app/repositories/item.py"
allowed_transformations = ["删除 owner 与 list 查询", "使用 SQLModel select、exec 与 col", "保留 flush 与 refresh"]
removed_references = ["owner_id、list_for_owner、count、分页"]
approved_deviation = ["事务由 session manager 独占"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/services/item.py"
target = "app/services/item.py"
allowed_transformations = ["删除 owner 检查与 list service"]
removed_references = ["owner_id、CurrentUser ownership、list"]
approved_deviation = ["缺失 Item 继续使用 legacy NotFoundError message 与 details"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlmodel_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/services/item.py"
target = "app/services/item.py"
allowed_transformations = ["删除 owner 检查与 list service", "使用 SQLModel AsyncSession"]
removed_references = ["owner_id、CurrentUser ownership、list"]
approved_deviation = ["缺失 Item 继续使用 legacy NotFoundError message 与 details"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/schemas/item.py"
target = "app/schemas/item.py"
allowed_transformations = ["删除 owner 与分页 schema", "内联仅 Item 使用的 BaseSchema 与 TimestampSchema"]
removed_references = ["owner_id、ItemList"]
approved_deviation = ["create、update、read 字段约束保持 legacy", "Item 关闭时不生成 ORM schema 支撑代码"]
verification = ["test_item_crud_switch_controls_the_complete_generated_slice"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/items.py"
target = "app/api/items.py"
allowed_transformations = ["删除 CurrentUser、owner 与 list endpoint", "路由前缀移至模块"]
removed_references = ["list endpoint、Query pagination、CurrentUser"]
approved_deviation = ["保留 create、get、update、delete HTTP contract"]
verification = ["test_both_orms_pass_real_postgresql_contract"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/alembic/versions/0021_create_items.py"
target = "alembic/versions/0021_create_items.py"
allowed_transformations = ["删除 owner 外键与 index", "重置为初始 migration"]
removed_references = ["owner_id、users.id ForeignKey、ix_items_owner_id、legacy down_revision"]
approved_deviation = ["保留 revision 0021_create_items 身份且 down_revision=None"]
verification = ["test_item_crud_switch_controls_the_complete_generated_slice", "test_both_orms_pass_real_postgresql_contract"]

# Issue #9 Redis consumers 与 Taskiq 独立 profile。

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "仅保留 Redis、cache 与 rate limit 依赖", "依赖版本兼容修改"]
removed_references = ["V1 排除能力依赖"]
approved_deviation = ["Logfire 关闭时不生成 instrumentation extras"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.env.example"
target = ".env.example"
allowed_transformations = ["Copier 模板语法", "仅保留启用 consumer 的环境变量"]
removed_references = ["V1 排除能力与 Taskiq 变量"]
approved_deviation = ["RATE_LIMIT_STORAGE 显式记录 redis"]
verification = ["test_issue_9_generation_boundaries"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/main.py"
target = "app/main.py"
allowed_transformations = ["删除 V1 排除生命周期", "保留 Redis、cache 与 limiter 生命周期"]
removed_references = ["用户、RAG、消息渠道、admin 与其他 V1 排除能力"]
approved_deviation = ["FastAPICache shutdown 显式 reset", "SlowAPI 使用 ASGI middleware 与统一异常 envelope"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/config.py"
target = "app/core/config.py"
allowed_transformations = ["删除 V1 排除 Settings", "保留 Redis 与 limiter Settings"]
removed_references = ["身份、billing、文件、RAG 与 provider Settings"]
approved_deviation = ["RATE_LIMIT_STORAGE 作为显式 Settings"]
verification = ["test_issue_9_generation_boundaries"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/clients/redis.py"
target = "app/clients/redis.py"
allowed_transformations = ["Copier 模板语法", "当前 redis asyncio close API"]
removed_references = ["无"]
approved_deviation = ["raw client 保持 binary 供 FastAPICache 使用，get 执行 UTF-8 decode"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/cache.py"
target = "app/core/cache.py"
allowed_transformations = ["Copier 模板语法", "增加 shutdown reset"]
removed_references = ["disabled no-op 分支"]
approved_deviation = ["文件仅在启用 cache 时生成"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/rate_limit.py"
target = "app/core/rate_limit.py"
allowed_transformations = ["Copier 模板语法", "增加 Redis storage URI", "保留 low/medium/high decorators"]
removed_references = ["disabled 空模块"]
approved_deviation = ["Redis rate limit 使用 settings.REDIS_URL 共享服务端状态"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/deps.py"
target = "app/api/deps.py"
allowed_transformations = ["删除 V1 排除 dependencies", "deployment API Key 替换", "保留 Redis dependency"]
removed_references = ["用户、JWT、teams 与排除 service dependencies"]
approved_deviation = ["Redis client 从 app lifespan state 注入"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/health.py"
target = "app/api/health.py"
allowed_transformations = ["根路径迁移", "删除排除资源 probes", "保留 Redis readiness"]
removed_references = ["RAG、provider、billing、Celery probes"]
approved_deviation = ["Redis probe 使用 dependency-injected RedisClient"]
verification = ["test_redis_consumers_pass_real_redis_contract"]

[[target]]
profile = "rate_limit_memory"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "仅保留 SlowAPI 依赖"]
removed_references = ["V1 排除能力与 Redis 依赖"]
approved_deviation = ["无"]
verification = ["test_memory_rate_limit_uses_no_redis"]

[[target]]
profile = "rate_limit_memory"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.env.example"
target = ".env.example"
allowed_transformations = ["Copier 模板语法", "仅保留 limiter 环境变量"]
removed_references = ["V1 排除能力与 Redis 变量"]
approved_deviation = ["RATE_LIMIT_STORAGE 显式记录 memory"]
verification = ["test_memory_rate_limit_uses_no_redis"]

[[target]]
profile = "rate_limit_memory"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/config.py"
target = "app/core/config.py"
allowed_transformations = ["删除 V1 排除 Settings", "保留 limiter Settings"]
removed_references = ["Redis 与其他未启用能力 Settings"]
approved_deviation = ["RATE_LIMIT_STORAGE 作为显式 Settings"]
verification = ["test_memory_rate_limit_uses_no_redis"]

[[target]]
profile = "rate_limit_memory"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/rate_limit.py"
target = "app/core/rate_limit.py"
allowed_transformations = ["Copier 模板语法", "显式 memory storage URI", "保留 low/medium/high decorators"]
removed_references = ["disabled 空模块"]
approved_deviation = ["memory:// 明确限制为 process-local storage"]
verification = ["test_memory_rate_limit_uses_no_redis"]

[[target]]
profile = "rate_limit_memory"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/main.py"
target = "app/main.py"
allowed_transformations = ["删除 V1 排除生命周期", "保留 SlowAPI middleware"]
removed_references = ["Redis 与其他未启用资源生命周期"]
approved_deviation = ["使用 SlowAPIASGIMiddleware 与统一异常 envelope"]
verification = ["test_memory_rate_limit_uses_no_redis"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "仅保留 Taskiq 与 taskiq-redis 依赖", "依赖版本兼容修改"]
removed_references = ["V1 排除 queue 与 API Redis client 依赖"]
approved_deviation = ["无"]
verification = ["test_taskiq_process_commands_start_and_stop_against_real_redis"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.env.example"
target = ".env.example"
allowed_transformations = ["Copier 模板语法", "仅保留 Taskiq 环境变量"]
removed_references = ["V1 排除能力与 API Redis client 变量"]
approved_deviation = ["result backend 变量命名为 TASKIQ_RESULT_BACKEND_URL"]
verification = ["test_issue_9_generation_boundaries"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/config.py"
target = "app/core/config.py"
allowed_transformations = ["删除 V1 排除 Settings", "保留独立 Taskiq URLs"]
removed_references = ["API Redis client 与其他未启用能力 Settings"]
approved_deviation = ["result backend 变量命名为 TASKIQ_RESULT_BACKEND_URL"]
verification = ["test_issue_9_generation_boundaries"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/worker/taskiq_app.py"
target = "app/worker/taskiq_app.py"
allowed_transformations = ["Copier 模板语法", "删除 legacy schedule import", "startup 与 shutdown 执行 Redis health", "阻塞队列读取不设置 socket read timeout"]
removed_references = ["RAG schedule 与虚构业务 task"]
approved_deviation = ["生命周期 hooks 同时验证 broker 与 result backend，pool 关闭仍由 Taskiq owner 管理", "Taskiq BRPOP 保持无限等待，避免 redis-py 默认读取超时终止空闲 worker"]
verification = ["test_taskiq_process_commands_start_and_stop_against_real_redis"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/cli/commands.py"
target = "cli/commands.py"
allowed_transformations = ["删除 V1 排除 commands", "保留 server 与 Taskiq commands"]
removed_references = ["用户、Celery、database 与 custom commands"]
approved_deviation = ["subprocess.run 使用 check=True"]
verification = ["test_taskiq_process_commands_start_and_stop_against_real_redis"]

# Issue #10 单轮 Agent 与 Pydantic AI-only Logfire profile。

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = "pyproject.toml"
allowed_transformations = ["Copier 模板语法", "仅保留 Pydantic AI、OpenAI-compatible 与 Logfire 依赖", "依赖版本兼容修改"]
removed_references = ["替代 Agent/provider SDK 与非 Pydantic AI Logfire extras"]
approved_deviation = ["OpenAI Chat Completions-compatible 模型接口", "Logfire base package 不请求不存在的 pydantic-ai extra"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults", "test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.env.example"
target = ".env.example"
allowed_transformations = ["Copier 模板语法", "仅保留 LLM_* 与 Logfire 环境变量"]
removed_references = ["provider 分支、conversation 与其他 V1 排除能力变量"]
approved_deviation = ["LLM_BASE_URL、LLM_API_KEY、LLM_MODEL 定义唯一模型接口"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/main.py"
target = "app/main.py"
allowed_transformations = ["删除 V1 排除生命周期", "挂载根级 Agent HTTP router", "configure-first Logfire"]
removed_references = ["WebSocket、conversation、用户/JWT 与非 Agent instrumentation"]
approved_deviation = ["Agent 使用根级 /agent/run 与 /agent/stream", "Logfire 仅在 Agent+Logfire profile 初始化"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/config.py"
target = "app/core/config.py"
allowed_transformations = ["删除 provider 与排除能力 Settings", "保留 LLM_* 与 Logfire Settings"]
removed_references = ["AI history、provider keys、RAG、skills 与 orchestration Settings"]
approved_deviation = ["单一 OpenAI Chat Completions-compatible 模型接口"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/exceptions.py"
target = "app/core/exceptions.py"
allowed_transformations = ["删除排除能力异常", "增加 Agent transport 失败封装"]
removed_references = ["用户/JWT、billing、文件与 conversation 异常"]
approved_deviation = ["AgentInvocationError 以稳定 502 envelope 隐藏上游细节"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/deps.py"
target = "app/api/deps.py"
allowed_transformations = ["删除身份与排除 service dependencies", "增加无状态 Agent factories"]
removed_references = ["CurrentUser、conversation、RAG、ask-user 与 provider dependencies"]
approved_deviation = ["deployment API Key 保护 Agent routes", "每次请求创建空 Deps"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/agents/assistant.py"
target = "app/agents/assistant.py"
allowed_transformations = ["Copier 模板语法", "删除 V1 排除 tools、provider 分支与多轮状态", "OpenAIChatModel 路径适配"]
removed_references = ["ask_user、RAG、charts、code、skills、todo、subagents、history 与 provider dispatch"]
approved_deviation = ["LLM_* 单一模型接口", "保留无状态 Deps、lazy Agent 与 current_datetime"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults", "test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/agents/prompts.py"
target = "app/agents/prompts.py"
allowed_transformations = ["删除已移除工具 guidance", "增加单轮调用约束"]
removed_references = ["ask-user、charts、code、RAG 与 deep research prompt"]
approved_deviation = ["Personality、Answering、Output 原文保留，仅追加 single-turn 约束"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/agents/utils.py"
target = "app/agents/utils.py"
allowed_transformations = ["删除未保留 helper"]
removed_references = ["无"]
approved_deviation = ["仅保留 current datetime helper"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/services/agent_invocation.py"
target = "app/services/agent.py"
allowed_transformations = ["删除 persistence、provider dispatch 与多轮 orchestration", "适配 JSON/SSE frame"]
removed_references = ["database messages、conversation、billing、provider routing 与 channel adapters"]
approved_deviation = ["最小 AgentInvocationService 只转换结果、事件与安全失败"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/routes/v1/agent.py"
target = "app/api/agent.py"
allowed_transformations = ["删除 WebSocket 与模型列表", "替换为单轮 JSON/SSE HTTP transport"]
removed_references = ["WebSocket、AgentSession、conversation、query auth 与模型列表"]
approved_deviation = ["POST /agent/run 与 POST /agent/stream 使用 deployment API Key"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/logfire_setup.py"
target = "app/core/telemetry.py"
allowed_transformations = ["删除非 Pydantic AI instrumentation", "增加默认内容采集禁用参数"]
removed_references = ["FastAPI、database、Redis、Taskiq 与 HTTPX instrumentation"]
approved_deviation = ["configure-first 后仅 instrument_pydantic_ai", "include_content 与 include_binary_content 均为 false"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_agents.py"
target = "tests/test_agent.py"
allowed_transformations = ["删除 provider、history 与排除 tools tests", "替换 transport 为本地 Chat-compatible fake server"]
removed_references = ["FunctionModel placeholder、configured-model tool、conversation 与 provider tests"]
approved_deviation = ["真实 OpenAIChatModel HTTP 路径验证 JSON、SSE、auth、validation、failure 与 stateless"]
verification = ["test_agent_and_logfire_pass_real_chat_completions_contract"]

[[target]]
profile = "agent_logfire"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/AGENTS.md"
target = "AGENTS.md"
allowed_transformations = ["backend 移至根目录", "删除 V1 排除 guidance", "增加批准的单轮 transport contract", "挂载 Issue #11 条件 deployment/CI guidance"]
removed_references = ["frontend、用户/JWT、conversation、RAG 与替代 providers"]
approved_deviation = ["记录 Agent JSON/SSE、LLM_* 与 content-safe Pydantic AI-only Logfire", "关闭 deployment/CI 时对应 guidance 不出现"]
verification = ["test_agent_and_logfire_generation_boundaries_follow_legacy_defaults"]

# Issue #11 deployment、CI、文档与 omp guidance profile。

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/Dockerfile"
target = "deploy/Dockerfile"
allowed_transformations = ["backend 移至项目根", "删除排除能力 runtime", "固定 Copier 路径与端口"]
removed_references = ["文件处理目录与额外 parser runtime"]
approved_deviation = ["healthcheck 使用 stdlib urllib", "uv image 固定为 0.8.4", "生成项目不含 uv.lock，因此不使用 --frozen 或 lockfile bind"]
verification = ["test_docker_profiles_restore_legacy_runtime_contract_and_conditions"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/.dockerignore"
target = "deploy/Dockerfile.dockerignore"
allowed_transformations = ["适配 root build context", "删除 backend-relative 路径"]
removed_references = ["删除能力目录与部署资产"]
approved_deviation = ["使用 Dockerfile-specific ignore 文件名"]
verification = ["test_docker_profiles_restore_legacy_runtime_contract_and_conditions"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docker-compose.yml"
target = "deploy/compose.yaml"
allowed_transformations = ["backend 移至项目根", "只保留 app、PostgreSQL、Redis 与 Taskiq services", "Copier 能力条件化"]
removed_references = ["删除能力 services、volumes、ports 与 commands"]
approved_deviation = ["service 名保持 db", "CLI 启动 Taskiq process"]
verification = ["test_docker_profiles_restore_legacy_runtime_contract_and_conditions"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docker-compose.dev.yml"
target = "deploy/compose.dev.yaml"
allowed_transformations = ["backend 移至项目根", "只保留 retained services", "源码挂载路径适配"]
removed_references = ["删除能力 services 与 development tooling"]
approved_deviation = ["使用 compose.dev.yaml 命名", "应用与 worker 通过项目 CLI 启动"]
verification = ["test_docker_profiles_restore_legacy_runtime_contract_and_conditions"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docker-compose.prod.yml"
target = "deploy/compose.prod.yaml"
allowed_transformations = ["backend 移至项目根", "只保留 retained services", "删除外部 proxy service"]
removed_references = ["删除能力 services、monitoring 与内置 proxy"]
approved_deviation = ["Nginx 作为外部配置", "生成默认值确保 compose config 可独立解析", "production bundled Redis 支持可选 REDIS_PASSWORD"]
verification = ["test_docker_profiles_restore_legacy_runtime_contract_and_conditions"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/nginx/nginx.conf"
target = "deploy/nginx.conf"
allowed_transformations = ["删除排除 upstream 与 routes", "backend root-layout 端口适配"]
removed_references = ["frontend、WebSocket 与 Flower"]
approved_deviation = ["upstream 使用 host.docker.internal 外部可达地址"]
verification = ["test_external_nginx_and_github_workflow_keep_retained_contract"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.github/workflows/ci.yml"
target = ".github/workflows/ci.yml"
allowed_transformations = ["backend 移至根目录", "删除排除 jobs", "基础设施与 Docker job 条件化"]
removed_references = ["frontend、security scan、pip-audit、Trivy 与 import-only smoke"]
approved_deviation = ["setup-uv 升级为 v6", "保留非阻断 Codecov", "coverage XML 仅报告、不设覆盖率门槛"]
verification = ["test_external_nginx_and_github_workflow_keep_retained_contract"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/README.md"
target = "README.md"
allowed_transformations = ["正文迁移为简体中文", "backend 移至根目录", "只保留 retained capability 章节"]
removed_references = ["删除产品能力、服务、命令与路径"]
approved_deviation = ["使用 uv 与三套 deploy/compose*.yaml 作为公开开发接口"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/ENV_VARS.md"
target = "ENV_VARS.md"
allowed_transformations = ["正文迁移为简体中文", "仅保留生成 Settings 变量", "关闭能力条件化"]
removed_references = ["删除能力环境变量"]
approved_deviation = ["LLM_* 使用批准的单一模型接口"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/SECURITY.md"
target = "SECURITY.md"
allowed_transformations = ["正文迁移为简体中文", "删除身份与排除能力安全模型", "保留 deployment API Key 与 TLS"]
removed_references = ["删除能力 hardening 与 known limitations"]
approved_deviation = ["安全边界改为 deployment API Key"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/CONTRIBUTING.md"
target = "CONTRIBUTING.md"
allowed_transformations = ["正文迁移为简体中文", "backend 移至根目录", "删除排除工具链"]
removed_references = ["frontend 与 pre-commit 专属步骤"]
approved_deviation = ["使用 Ruff 100 字符与 ty"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/MANUAL_STEPS.md"
target = "MANUAL_STEPS.md"
allowed_transformations = ["正文迁移为简体中文", "只保留 selected external services", "关闭能力条件化"]
removed_references = ["删除能力账号、secret 与部署步骤"]
approved_deviation = ["健康 contract 使用根级 probes"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/adding_features.md"
target = "docs/adding_features.md"
allowed_transformations = ["正文迁移为简体中文", "路径适配", "仅保留 retained extension seams"]
removed_references = ["删除能力示例与替代 Agent 实现"]
approved_deviation = ["示例收敛为当前公开分层"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/architecture.md"
target = "docs/architecture.md"
allowed_transformations = ["正文迁移为简体中文", "backend 移至根目录", "删除排除层级"]
removed_references = ["身份、文件、conversation 与其他删除能力架构"]
approved_deviation = ["记录 deployment API Key 与无状态 Agent 边界"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/commands.md"
target = "docs/commands.md"
allowed_transformations = ["正文迁移为简体中文", "删除 Make interface", "仅保留真实 CLI 与 compose 命令"]
removed_references = ["删除能力与替代队列命令"]
approved_deviation = ["命令从项目根执行"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/configuration.md"
target = "docs/configuration.md"
allowed_transformations = ["正文迁移为简体中文", "仅保留生成 Settings", "变量名适配"]
removed_references = ["身份、provider 与删除能力配置"]
approved_deviation = ["LLM_* 单一模型接口与 deployment API Key"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/patterns.md"
target = "docs/patterns.md"
allowed_transformations = ["正文迁移为简体中文", "删除排除 patterns", "保留 DI/service/repository/schema"]
removed_references = ["frontend、connector 与身份资源示例"]
approved_deviation = ["示例使用无身份 Widget"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/testing.md"
target = "docs/testing.md"
allowed_transformations = ["正文迁移为简体中文", "根目录命令适配", "按 selected capability 条件化"]
removed_references = ["删除能力测试说明"]
approved_deviation = ["明确真实基础设施与公开 seam"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/deploy.md"
target = "docs/deploy.md"
allowed_transformations = ["正文迁移为简体中文", "删除排除平台与资产", "root-layout Docker 路径适配"]
removed_references = ["frontend、Kubernetes 与平台专属步骤"]
approved_deviation = ["仅文档化三套 compose 与外部 Nginx"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/AGENTS.md"
target = "AGENTS.md"
allowed_transformations = ["backend 移至根目录", "删除排除 guidance", "增加 selected deployment/CI 路径"]
removed_references = ["删除能力 agent context"]
approved_deviation = ["保留单一根级共享 context"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/api-conventions.md"
target = ".omp/rules/api-conventions.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "globs 路径适配"]
removed_references = ["身份与删除 routes 约定"]
approved_deviation = ["使用 omp description/globs frontmatter"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/architecture.md"
target = ".omp/rules/architecture.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "删除排除层级"]
removed_references = ["删除能力 architecture guidance"]
approved_deviation = ["记录 retained resource ownership"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/code-style.md"
target = ".omp/rules/code-style.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "工具与路径适配"]
removed_references = ["删除技术栈 style"]
approved_deviation = ["使用 Ruff、ty 与 100 字符"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/exceptions-security.md"
target = ".omp/rules/exceptions-security.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "删除身份异常"]
removed_references = ["身份、authorization 与删除能力异常"]
approved_deviation = ["deployment API Key 与统一 envelope"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/schemas-models.md"
target = ".omp/rules/schemas-models.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "删除排除 model guidance"]
removed_references = ["身份与删除资源 schema"]
approved_deviation = ["保留 Create/Update/Response contract"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/rules/testing.md"
target = ".omp/rules/testing.md"
allowed_transformations = ["转换为 omp rule", "正文迁移为简体中文", "测试 seam 适配"]
removed_references = ["删除能力测试 guidance"]
approved_deviation = ["强调真实 service 与公开 seam"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/skills/pytest-suite/SKILL.md"
target = ".omp/skills/pytest-suite/SKILL.md"
allowed_transformations = ["转换为 omp skill", "正文迁移为简体中文", "命令路径适配"]
removed_references = ["删除能力 fixture guidance"]
approved_deviation = ["显式 name/description frontmatter"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/skills/alembic-migration/SKILL.md"
target = ".omp/skills/alembic-migration/SKILL.md"
allowed_transformations = ["转换为条件 omp skill", "正文迁移为简体中文", "CLI 路径适配"]
removed_references = ["删除 models 与 seed 流程"]
approved_deviation = ["仅 PostgreSQL 时生成"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/skills/background-task/SKILL.md"
target = ".omp/skills/background-task/SKILL.md"
allowed_transformations = ["转换为条件 omp skill", "正文迁移为简体中文", "仅保留 Taskiq"]
removed_references = ["替代 queues 与示例 task"]
approved_deviation = ["仅 Taskiq 时生成"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/skills/agent-tool/SKILL.md"
target = ".omp/skills/agent-tool/SKILL.md"
allowed_transformations = ["转换为条件 omp skill", "正文迁移为简体中文", "仅保留无状态 Pydantic AI"]
removed_references = ["provider 分支、history 与排除 tools"]
approved_deviation = ["仅 Pydantic AI 时生成"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/commands/add-endpoint.md"
target = ".omp/commands/add-endpoint.md"
allowed_transformations = ["转换为 omp command", "正文迁移为简体中文", "参数 placeholder 适配"]
removed_references = ["身份与删除能力 endpoint guidance"]
approved_deviation = ["使用当前 omp 支持的 $ARGUMENTS；不保留未消费 metadata"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/commands/fix-issue.md"
target = ".omp/commands/fix-issue.md"
allowed_transformations = ["转换为 omp command", "正文迁移为简体中文", "移除 permission allowlist"]
removed_references = ["Claude-specific invocation 与 permissions"]
approved_deviation = ["使用当前 omp 支持的 $ARGUMENTS；不保留未消费 metadata"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.claude/commands/review.md"
target = ".omp/commands/review.md"
allowed_transformations = ["转换为 omp command", "正文迁移为简体中文", "审查标准适配"]
removed_references = ["Claude-specific invocation 与 permissions"]
approved_deviation = ["使用当前 omp 支持的 $ARGUMENTS；不保留未消费 metadata"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/howto/add-api-endpoint.md"
target = "docs/howto/add-api-endpoint.md"
allowed_transformations = ["正文迁移为简体中文", "route 与 dependency 适配 root layout", "按数据库能力条件化"]
removed_references = ["Notification 示例、用户/JWT、email channel、list endpoint 与 Makefile 命令"]
approved_deviation = ["改为 retained capability 的 contract-first 操作步骤"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/howto/add-agent-tool.md"
target = "docs/howto/add-agent-tool.md"
allowed_transformations = ["正文迁移为简体中文", "只保留 Pydantic AI tool contract", "改用 RunContext dependency"]
removed_references = ["替代 agent framework、email 与外部 weather 示例"]
approved_deviation = ["按无状态单轮 Agent contract 验证 JSON 与 SSE"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/howto/add-background-task.md"
target = "docs/howto/add-background-task.md"
allowed_transformations = ["正文迁移为简体中文", "只保留 Taskiq", "命令适配项目 CLI"]
removed_references = ["Celery、ARQ、Prefect、Flower、通知渠道与 Makefile 命令"]
approved_deviation = ["使用当前 broker 与 scheduler process contract"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

[[target]]
profile = "delivery"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/docs/howto/customize-agent-prompt.md"
target = "docs/howto/customize-agent-prompt.md"
allowed_transformations = ["正文迁移为简体中文", "只保留无状态 Pydantic AI prompt contract"]
removed_references = ["RAG、persona、用户支持与 AI_TEMPERATURE"]
approved_deviation = ["增加 credential boundary 与 content-safe Logfire 指引"]
verification = ["test_docs_and_omp_guidance_follow_generated_capabilities"]

# Issue #12 root integration、package markers 与生成项目验收测试。

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/.gitignore"
target = ".gitignore"
allowed_transformations = ["删除 frontend 与排除能力 ignore 项", "生成项目根目录适配"]
removed_references = ["frontend、Node、文件上传与删除部署资产"]
approved_deviation = ["仅保留生成后端项目会产生的本地文件"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/pyproject.toml"
target = ".python-version"
allowed_transformations = ["backend 移至项目根", "Copier python_version 问题物化"]
removed_references = ["frontend runtime version"]
approved_deviation = ["使用 uv 识别的独立 .python-version 文件"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/alembic.ini"
target = "alembic.ini"
allowed_transformations = ["backend 移至项目根", "删除身份相关 migration 配置"]
removed_references = ["删除能力 migration 路径与命名"]
approved_deviation = ["保留单一清理后 migration chain"]
verification = ["test_both_orms_pass_real_postgresql_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/__init__.py"
target = "app/__init__.py"
allowed_transformations = ["backend 移至项目根", "Copier 变量语法"]
removed_references = ["删除能力 package 引用"]
approved_deviation = ["package docstring 使用生成项目名"]
verification = ["test_named_profiles_cover_every_valid_capability_pair", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/api/__init__.py"
target = "app/api/__init__.py"
allowed_transformations = ["backend 移至项目根", "删除排除 route 引用"]
removed_references = ["用户、消息、文件、RAG 与 demo routes"]
approved_deviation = ["仅保留 package marker"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/core/__init__.py"
target = "app/core/__init__.py"
allowed_transformations = ["backend 移至项目根", "删除排除 core 引用"]
removed_references = ["删除能力 core utilities"]
approved_deviation = ["仅保留 package marker"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/schemas/__init__.py"
target = "app/schemas/__init__.py"
allowed_transformations = ["backend 移至项目根", "删除排除 schema 引用"]
removed_references = ["用户、分页、消息、文件与 RAG schemas"]
approved_deviation = ["仅保留 package marker"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/services/__init__.py"
target = "app/services/__init__.py"
allowed_transformations = ["backend 移至项目根", "删除排除 service 引用"]
removed_references = ["身份、消息、文件、Webhooks 与 RAG services"]
approved_deviation = ["仅保留 package marker"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/app/worker/__init__.py"
target = "app/worker/__init__.py"
allowed_transformations = ["backend 移至项目根", "仅在 Taskiq profile 生成"]
removed_references = ["Celery、ARQ、demo tasks 与 schedule package"]
approved_deviation = ["仅保留 Taskiq worker package marker"]
verification = ["test_taskiq_process_commands_start_and_stop_against_real_redis", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/cli/__init__.py"
target = "cli/__init__.py"
allowed_transformations = ["backend 移至项目根", "删除排除 CLI 引用"]
removed_references = ["用户与删除能力 commands"]
approved_deviation = ["仅保留 package marker"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/__init__.py"
target = "tests/__init__.py"
allowed_transformations = ["backend 移至项目根", "Copier 变量语法"]
removed_references = ["删除能力测试 package 引用"]
approved_deviation = ["package docstring 使用生成项目名"]
verification = ["test_named_profiles_cover_every_valid_capability_pair", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/conftest.py"
target = "tests/conftest.py"
allowed_transformations = ["backend 移至项目根", "删除排除 fixtures", "按保留能力条件化"]
removed_references = ["用户、JWT、文件、消息、RAG 与删除数据库 fixtures"]
approved_deviation = ["HTTPX lifespan client 与 rate limiter teardown 适配批准后的公开 seam"]
verification = ["test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/api/test_health.py"
target = "tests/test_api.py"
allowed_transformations = ["backend 移至项目根", "合并保留 health、API Key 与异常 contract"]
removed_references = ["用户/JWT、分页与删除 routes"]
approved_deviation = ["deployment API Key 与统一 failure envelope 在同一公开 HTTP seam 验收"]
verification = ["test_minimal_project_passes_http_and_cli_runtime_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_core.py"
target = "tests/test_cache.py"
allowed_transformations = ["backend 移至项目根", "仅保留 FastAPICache 初始化 contract"]
removed_references = ["cache demo endpoint 与删除能力 core tests"]
approved_deviation = ["通过公开 cache backend 验证共享 Redis client"]
verification = ["test_redis_consumers_pass_real_redis_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "postgresql_sqlalchemy_items"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_repositories.py"
target = "tests/test_items.py"
allowed_transformations = ["backend 移至项目根", "删除 owner/user 引用", "仅保留 Item create/get/update/delete"]
removed_references = ["用户 foreign key、分页与 Item list"]
approved_deviation = ["通过批准后的 Item HTTP contract 验证双 ORM 分支"]
verification = ["test_both_orms_pass_real_postgresql_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_core.py"
target = "tests/test_rate_limit.py"
allowed_transformations = ["backend 移至项目根", "保留 SlowAPI memory/Redis storage contract"]
removed_references = ["/limited demo route 与删除能力 core tests"]
approved_deviation = ["通过临时受保护 route 验证 429 failure envelope"]
verification = ["test_redis_consumers_pass_real_redis_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "redis_consumers"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_clients.py"
target = "tests/test_redis.py"
allowed_transformations = ["backend 移至项目根", "使用真实 Redis 替代内部 mock"]
removed_references = ["未保留的 exists helper 与其他 clients"]
approved_deviation = ["验证 connect、ping、get、set、delete 与 close lifecycle"]
verification = ["test_redis_consumers_pass_real_redis_contract", "test_each_pairwise_profile_has_protected_legacy_equivalence"]

[[target]]
profile = "taskiq"
legacy_source = "legacy/template/{{cookiecutter.project_slug}}/backend/tests/test_worker_taskiq.py"
target = "tests/test_taskiq.py"
allowed_transformations = ["backend 移至项目根", "删除 demo task discovery", "验证 broker/result/scheduler lifecycle"]
removed_references = ["定时 health task 与业务 demo task"]
approved_deviation = ["不为测试新增 task；只验证公开 worker 配置"]
verification = ["test_taskiq_process_commands_start_and_stop_against_real_redis", "test_each_pairwise_profile_has_protected_legacy_equivalence"]
