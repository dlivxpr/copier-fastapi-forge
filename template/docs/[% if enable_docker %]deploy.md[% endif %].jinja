# 部署

## 资产

- `deploy/Dockerfile`：multi-stage、non-root、内置 `HEALTHCHECK` 的 backend image。
- `deploy/compose.yaml`：默认单机编排。
- `deploy/compose.dev.yaml`：源码挂载与 reload。
- `deploy/compose.prod.yaml`：生产环境编排。
[% if reverse_proxy == "nginx_external" %]- `deploy/nginx.conf`：外部 TLS reverse proxy。
[% endif %]

build context 是项目根目录；Dockerfile 路径是 `deploy/Dockerfile`。

## 配置与启动

```bash
cp .env.example .env
docker compose -f deploy/compose.prod.yaml config
docker compose -f deploy/compose.prod.yaml build
docker compose -f deploy/compose.prod.yaml up -d
```

生产启动前必须替换生成默认值，尤其是 `API_KEY`[% if database == "postgresql" %] 与 `POSTGRES_PASSWORD`[% endif %][% if ai_framework == "pydantic_ai" %]、`LLM_API_KEY`[% endif %]。
[% if database == "postgresql" %]

启动后执行 migration：

```bash
docker compose -f deploy/compose.prod.yaml exec app python -m cli.commands db upgrade
```
[% endif %]

## 健康检查

```bash
curl http://localhost:[[ backend_port ]]/health
curl http://localhost:[[ backend_port ]]/health/live
curl http://localhost:[[ backend_port ]]/health/ready
```

`/health/ready` 会检查当前启用的依赖。只有该 endpoint 正常后才接入业务流量。
[% if reverse_proxy == "nginx_external" %]

## 外部 Nginx

1. 将 certificate 与 private key 挂载到 `/etc/nginx/ssl/cert.pem` 和 `/etc/nginx/ssl/key.pem`。
2. 将 ACME webroot 挂载到 `/var/www/certbot`。
3. 让 `host.docker.internal` 解析到 backend host；Linux Docker 可使用 `--add-host host.docker.internal:host-gateway`。
4. 将 `deploy/nginx.conf` 挂载到 `/etc/nginx/conf.d/default.conf`，执行 `nginx -t`。
5. 开放 80/443，确认 HTTP redirect、TLS 和 forwarded headers。
[% endif %]

## 部署后检查

- [ ] `/health`、`/health/live`、`/health/ready` 返回成功。
[% if database == "postgresql" %]- [ ] Alembic current revision 正确。
[% endif %][% if background_tasks == "taskiq" %]- [ ] worker 与 scheduler process 正常连接 Redis。
[% endif %]- [ ] 错误 API Key 被拒绝，正确 API Key 可访问业务 route。
- [ ] 日志和 telemetry 不含 secret。
